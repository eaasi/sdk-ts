{
  "name": "@eaasi/sdk-ts",
  "version": "0.1.0",
  "description": "TypeScript SDK for the EAASI platform",
  "license": "Apache-2.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check:types": "tsc --noEmit",
    "clean": "rm -rv ./dist"
  },
  "dependencies": {
    "openapi-fetch": "^0.13.5"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3"
  }
}
